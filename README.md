# Enterprise-Web-Scraping-Lead-Generation-System
I built a scalable system that automatically collects data from websites, handles pagination and blocking, removes duplicates, and ensures accuracy. It extracts key data and saves it in clean CSV, JSON, and database formats, making it ready for business, research, or lead generation use.
